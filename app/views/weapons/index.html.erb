<% @weapon_collection.in_groups_of(3, false).each do |group| %>
  <% group.each do |weapon| %>
    <%= link_to(image_tag(weapon.image.medium.url), weapon_path(weapon)) if weapon.image? %>
    <%= weapon.name %>
    C$ <%= '%.2f' % weapon.price %>
  <% end %>
<% end %>